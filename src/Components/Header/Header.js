import React from 'react';
import './Header.css'
const Header = () => {
    return (
        <div className="header-container">
            <h1>This site owner is pure beginner and he is so lazy thats because site like this :)</h1>
        </div>
    );
};

export default Header;